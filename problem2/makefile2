CPP = g++
CFLAGS = -Wall --pedantic-errors -02
SOURCES = $(wildcard *.cpp)
HEADERS = $(wildcard *.h)
OBJECTS = $(SOURCES:%.cpp=%.o)

program: ${OBJECTS}
	${CPP} ${CFLAGS} ${OBJECTS} -o program
%.o: %.cpp ${HEADERS}
	${CPP} ${CFLAGS} -c $<
.PHONY: clean
clean:
	-@rm -f program
	-@rm -f ${OBJECTS}
